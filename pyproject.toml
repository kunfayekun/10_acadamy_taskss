[project]
name = "project-chimera"
version = "0.1.0"
description = "Project Chimera - agentic analysis platform (minimal pyproject)"
requires-python = ">=3.11"

dependencies = [
    "pytest>=7.4.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
